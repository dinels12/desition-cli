{
  "version": "0.1.4",
  "license": "MIT",
  "main": "dist/index.js",
  "engines": {
    "node": ">=14"
  },
  "bin": "dist/index.js",
  "scripts": {
    "start": "node dist/index",
    "build": "rimraf ./build && tsc",
    "prepare": "rimraf ./build && tsc",
    "clean": "rimraf ./build"
  },
  "name": "@dinels/desition-cli",
  "author": "dinels",
  "devDependencies": {
    "@types/shelljs": "^0.8.9",
    "typescript": "^4.4.4"
  },
  "dependencies": {
    "rimraf": "^3.0.2",
    "shelljs": "^0.8.4"
  }
}
